<?php

/**
 * @file
 * template.php
 */

/*function bootstrap_subtheme_preprocess_html(&$variables) {
	//$node =& $variables['node'];
	//if ($node['type'] == 'product_kit') {
			$options = array(
					'group' => JS_THEME,
			);
			drupal_add_js(drupal_get_path('theme', 'bootstrap_subtheme'). '/bootstrap/js/three.js', $options);
			drupal_add_js(drupal_get_path('theme', 'bootstrap_subtheme'). '/bootstrap/js/detector.js', $options);
			drupal_add_js(drupal_get_path('theme', 'bootstrap_subtheme'). '/bootstrap/js/stats.js', $options);
		//}
}*/
